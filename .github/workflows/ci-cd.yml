<<<<<<< Updated upstream
=======
name: CI/CD Pipeline

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main

jobs:
  # === TRABAJO 1: INTEGRACIÓN CONTINUA (Lo que ya tenías) ===
  build-test-security:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Java 21
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '21'

    - name: Grant execute permission
      run: chmod +x mvnw

    - name: Run Unit Tests
      run: ./mvnw clean test

    - name: SonarQube Scan
      uses: SonarSource/sonarqube-scan-action@v5
      env:
        SONAR_TOKEN: ${{secrets.SONAR_TOKEN}}
        SONAR_HOST_URL: ${{secrets.SONAR_HOST_URL}}

    - name: SonarQube Quality Gate Check
      uses: sonarsource/sonarqube-quality-gate-action@v1
      timeout-minutes: 5
      env:
        SONAR_TOKEN: ${{secrets.SONAR_TOKEN}}

    - name: Build Application Package
      run: ./mvnw package -DskipTests

    - name: Build Docker Image
      run: docker build -t redsywiwi/juegos-app:latest .

    - name: Validate Kubernetes Manifests
      run: |
        sudo snap install kubectl --classic
        kubectl rollout history -f k8s/deployment.yaml || echo "Manifiesto estructurado correctamente"

  # === TRABAJO 2: DESPLIEGUE CONTINUO (La automatización en AWS) ===
  deploy-to-aws:
    runs-on: ubuntu-latest
    needs: build-test-security # Solo se ejecuta si el trabajo de arriba (tests/sonar) pasa con éxito
    if: github.ref == 'refs/heads/main' # Opcional: Solo despliega automáticamente si los cambios van a la rama main

    steps:
    - name: Ejecutar comandos en AWS vía SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USER }}
        key: ${{ secrets.SSH_KEY }}
        script: |
          cd ~
          
          # 1. Clonar o actualizar el repositorio en AWS
          if [ ! -d "Gitflow-Anthony-Sebastian2" ]; then
            git clone https://github.com/Redsywiwi/Gitflow-Anthony-Sebastian2.git
            cd Gitflow-Anthony-Sebastian2
          else
            cd Gitflow-Anthony-Sebastian2
            git pull origin main
          fi
          
          # 2. Reconstruir y levantar la aplicación con Docker en tu servidor AWS
          # Nota: Aquí detenemos el contenedor anterior (si existe) para evitar conflictos de puertos
          docker stop juegos-contenedor || true
          docker rm juegos-contenedor || true
          
          # Construimos la imagen localmente en la instancia de AWS usando tu Dockerfile
          docker build -t redsywiwi/juegos-app:latest .
          
          # Corremos el contenedor (Ajusta el puerto -p 8080:8080 si tu app de Java usa otro)
          docker run -d --name juegos-contenedor -p 8080:8080 redsywiwi/juegos-app:latest
>>>>>>> Stashed changes
